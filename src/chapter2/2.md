# let
letキーワードは、変数宣言に使用します。

```rust
fn main() {
    let x = 100;
    let t = "rust";
}
```

デフォルトで不変ですが、`mut`を付けることで可変にできます。
```rust
fn main() {
    let mut x = 100;
    println!("{}", x); // 100

    x = 50;
    println!("{}", x); // 50
}